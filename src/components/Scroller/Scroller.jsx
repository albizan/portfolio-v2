import React from 'react';
import { MouseBox, MouseIcon, ScrollingItem } from './styled';

const Scroller = () => {
  return (
    <MouseBox>
      <MouseIcon>
        <ScrollingItem />
      </MouseIcon>
    </MouseBox>
  );
};

export default Scroller;
